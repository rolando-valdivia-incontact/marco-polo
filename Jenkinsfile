@Library('cicd-pipeline-templates@BeforeCoreAfter') _
import com.inContact.pipeline.*

basicPipeline {
    template = 'nodejs'
    deployType = 'docker'
    packaginType = 'docker'
    deployGate = true
}